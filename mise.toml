[settings]
experimental = true

# Each env owning an installer declares its own `setup:` task, because two envs
# cannot both define `bootstrap`: the second is silently dropped.
# An env list naming neither zsh nor agents fails here rather than skipping
# an installer in silence.
[tasks.bootstrap]
depends = ["setup:*"]
